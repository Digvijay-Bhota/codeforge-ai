# mcp/

> **Status: Planned (Phase 4+)**

This directory will contain Model Context Protocol (MCP) tool servers.

Planned responsibilities:
- Wrapping external tools (GitHub API, file system, shell) as MCP-compatible servers
- Providing a clean tool interface to the agent runtime
- Sandboxed execution of tool calls

Nothing is implemented here yet.
